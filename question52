class Solution {
    public int countSubarrays(int[] nums) {
        int cnt = 0;
        int i=0;
        int j=2;
        while(j<nums.length){
            if((nums[i]+nums[j])*2==nums[i+1])cnt++;
            i++;
            j++;
        }

        return cnt;
    }
}